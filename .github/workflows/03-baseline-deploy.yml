# =============================================================================
# 03 – Baseline Deploy — MUSS NACH Workflow 02 (Tailscale-Bootstrap) laufen!
#   Vorbedingung: Ziel-VPS hat Tailscale-IP (per API geprüft, sonst Abbruch).
#   Läuft über Tailscale (Public-IP ist nach 02 geschlossen).
# Siehe docs/workflows/deploy-stages.md
# =============================================================================
name: "03 – Baseline Deploy (Phase 1, via Tailscale-IP)"

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Ziel-VPS'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod

permissions:
  contents: read

jobs:
  baseline:
    name: "Phase 1 – System-Baseline"
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      VPS_USER: ${{ secrets.VPS_USER }}
      SSH_KEY: ${{ secrets.SSH_KEY }}

    steps:
      - uses: actions/checkout@v4

      - name: Vorbedingungen prüfen (Reihenfolge-Gate)
        env:
          TS_CLIENT_ID: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          TS_CLIENT_SECRET: ${{ secrets.TAILSCALE_OAUTH_CLIENT_SECRET }}
          TS_TAILNET: ${{ secrets.TAILSCALE_TAILNET }}
          TS_API_KEY: ${{ secrets.TAILSCALE_API_KEY }}
        run: |
          FAIL=0
          [ -n "$SSH_KEY" ] || { echo "❌ Secret SSH_KEY fehlt – Workflow 00 (SSH-Key generieren) zuerst ausführen"; FAIL=1; }
          [ -n "$VPS_USER" ] || { echo "❌ Secret VPS_USER fehlt"; FAIL=1; }
          [ -n "$TS_CLIENT_ID" ] || { echo "❌ Secret TAILSCALE_OAUTH_CLIENT_ID fehlt – Workflow 01 (Terraform) zuerst ausführen"; FAIL=1; }
          [ -n "$TS_CLIENT_SECRET" ] || { echo "❌ Secret TAILSCALE_OAUTH_CLIENT_SECRET fehlt – Workflow 01 (Terraform) zuerst ausführen"; FAIL=1; }
          [ -n "$TS_TAILNET" ] || { echo "❌ Secret TAILSCALE_TAILNET fehlt"; FAIL=1; }
          [ -n "$TS_API_KEY" ] || { echo "❌ Secret TAILSCALE_API_KEY fehlt – Workflow 01 (Terraform) zuerst ausführen"; FAIL=1; }
          [ "$FAIL" -eq 0 ] || { echo "❌ Vorbedingungen nicht erfüllt – Abbruch"; exit 1; }
          echo "✅ Vorbedingungen ok (SSH_KEY, VPS_USER, OAuth, Tailnet, API-Key)"

      - name: Connect Runner to Tailnet (MagicDNS)
        uses: tailscale/github-action@v4.1.3
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_OAUTH_CLIENT_SECRET }}
          tags: tag:ci

      - name: Netmap-Sync abwarten
        run: sleep 15

      - name: VPS Tailscale-IP ermitteln (Tailscale-API)
        env:
          TS_TAILNET: ${{ secrets.TAILSCALE_TAILNET }}
          TS_API_KEY: ${{ secrets.TAILSCALE_API_KEY }}
        run: |
          NODE="vps-${{ inputs.target }}"
          TS_IP=$(curl -s \
            "https://api.tailscale.com/api/v2/tailnet/${TS_TAILNET}/devices?fields=hostname,addresses" \
            -H "Authorization: Bearer ${TS_API_KEY}" | jq -r --arg n "$NODE" '[.devices[] | select(.hostname == $n or .hostname == ($n + "-1"))] | sort_by(.lastSeen // "") | reverse | .[0].addresses[0] // empty' | head -1)
          if [ -z "$TS_IP" ] || [ "$TS_IP" = "null" ]; then
            echo "❌ Keine Tailscale-IP für Node ${NODE} gefunden."
            echo "   Vorbedingung nicht erfüllt: Workflow 02 (Tailscale-Bootstrap) muss zuerst laufen!"
            exit 1
          fi
          echo "VPS_TS_IP=${TS_IP}" >> $GITHUB_ENV
          echo "✅ Node ${NODE} → ${TS_IP}"

      - name: SSH-Key vorbereiten
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "$VPS_TS_IP" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Ansible installieren
        run: sudo apt-get update -qq && sudo apt-get install -y -qq ansible


      - name: Inventory-Datei erstellen (MagicDNS)
        run: |
          echo "[vps]" > /tmp/inventory
          echo "vps-${{ inputs.target }} ansible_host=$VPS_TS_IP ansible_user=$VPS_USER" >> /tmp/inventory
          cat /tmp/inventory

      - name: Phase 1 – Ansible Baseline ausführen
        working-directory: ansible
        run: |
          ansible-playbook playbooks/01-baseline.yml \
            -i /tmp/inventory

      - name: Post-Deploy Check (SSH via Tailscale)
        run: |
          echo "✅ Phase 1 abgeschlossen"
          echo "SSH-Verifikation via MagicDNS:"
          ssh -o StrictHostKeyChecking=accept-new -o ConnectTimeout=10 \
              "$VPS_USER@$VPS_TS_IP" \
              "hostname; tailscale ip -4" || { echo "❌ SSH via Tailscale fehlgeschlagen"; exit 1; }
