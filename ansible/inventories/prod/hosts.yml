all:
  children:
    vps:
      hosts:
        vps-prod:
          ansible_host: vps-prod.tailcfea8a.ts.net
          ansible_user: openclaw
          ansible_ssh_private_key_file: "{{ lookup('env', 'VPS_SSH_KEY_PATH') | default('~/.ssh/id_ed25519', true) }}"
