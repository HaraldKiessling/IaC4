---
# Traefik-Rolle – Defaults (ADR-017/018/019/020)
traefik_version: "v3.7.9"   # Pinning ADR-017 (group_vars SSoT; hier Fallback)

docker_network: "traefik-network"   # Fallback (SSoT: group_vars/all.yml)
