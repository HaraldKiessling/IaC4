# 2026-08-03

## Design 06 (Multi-Agent-Orchestrierung)
- Root-Cause: agents.list ohne System-Prompts → Orchestrator macht ~90 % selbst
- Fix: Per-Agent-Workspaces + Rollen-AGENTS.md + Orchestrator-Tool-Scoping (strict: deny exec/web_search/web_fetch)
- Pilot d06/d07: OC3-Orchestrator-Anteil 24,6 % / 28,0 % (< 40 %-Ziel erreicht); BP-7-Falle gelöst; Artefakt 0/6 → 6/6

## Memory-Setup (final, 2026-08-03)
- Backend: `memory.backend: qmd` + `memorySearch.provider: "local"` (konsistent mit PROD-Host)
- QMD 2.5.3 wird im Container installiert (`npm install -g @tobilu/qmd@2.5.3`, User-npm ~/.npm-global, PATH via Compose)
- MEMORY.md + memory/ werden nach Clean deployt (Ansible, alle Instanzen)
- FTS-only-Entscheidung: `provider: local` (QMD-lokal), kein Remote-Embedding — QMD nutzt lokale Modelle

## Entscheidungen
- Tool-Scoping: scharf (strict) als Default, Fallback soft per group_var
- GH-Read-Token: `.gh-read-token`-Datei (0600, Agent-lesbar) + GH_TOKEN-Env (gh-CLI) — beide Wege dokumentiert
