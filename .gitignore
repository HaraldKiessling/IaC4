# OS
.DS_Store
Thumbs.db
*.swp
*.swo

# IDE
.idea/
.vscode/
*.code-workspace

# Python/Ansible
__pycache__/
*.pyc
*.retry
.inventory_*
*.log

# Secrets
.env
!*.example
*.pem
*.key
oauth-creds.txt
.secrets/
.ssh_vps/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup

# Build/Artifacts
dist/
build/
*.tar.gz
*.zip

# Temp
/tmp/

# ACL-Rohexport (roher Live-Stand) – wird NICHT versioniert.
# Quelle: `scripts/ensure-acl.py --export` (read-only GET) / Workflow 00 (export=true).
# Ablage stattdessen als Artefakt (Workflow) + eingefrorene Kopie im Workspace.
# Siehe docs/workflows/acl-migration-runbook.md (Entscheid 3, 2026-09-11).
acl/live*.hujson
acl/live*.hujson.sha256
